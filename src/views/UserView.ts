import type { _ProfileAttributes, _UserAttributes } from "@voxtl/types"

export class UserView {
    constructor() {
        //
    }

    format(params: [_UserAttributes, _ProfileAttributes]) {
        params[0]
    }
}